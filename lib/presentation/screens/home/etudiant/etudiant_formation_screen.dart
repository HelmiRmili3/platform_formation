import 'package:flutter/material.dart';

class EtudiantFormationScreen extends StatelessWidget {
  const EtudiantFormationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('EtudiantFormationScreen'),
    );
  }
}
