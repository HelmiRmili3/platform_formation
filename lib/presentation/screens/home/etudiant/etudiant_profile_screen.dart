import 'package:flutter/material.dart';

class EtudiantProfileScreen extends StatelessWidget {
  const EtudiantProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Etudiant Profile Screen"),
    );
  }
}
