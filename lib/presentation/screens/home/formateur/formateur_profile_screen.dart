import 'package:flutter/material.dart';

class FormateurProfileScreen extends StatelessWidget {
  const FormateurProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FormateurProfileScreen"),
    );
  }
}
